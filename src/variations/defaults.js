const defaults = {
  bgColor: '#112233',
  autoRotate: false,
  lightness: 5,
  chart: '1',
  duration: 2,
  impacts: 'height',
  itemModifier: 1,
  itemOperation: '*',
  gapAxis: 'x',
  gapModifier: 0.1,
  gapOperation: '*',
}

export default defaults
